<configure
    xmlns="http://namespaces.zope.org/zope"
    xmlns:browser="http://namespaces.zope.org/browser"
    xmlns:plone="http://namespaces.plone.org/plone"
    i18n_domain="plonetheme.kaerumy">

  <!-- Set overrides folder for Just-a-Bunch-Of-Templates product -->
  <!-- Include the full z3c.jbot configuration: it registers the jbot
       metadirectives (meta.zcml) and installs the runtime template
       patches (z3c.jbot.patches) that make the overrides effective. -->
  <include package="z3c.jbot" />
  <browser:jbot
      directory="overrides"
      layer="plonetheme.kaerumy.interfaces.IPlonethemeKaerumyLayer"
      />

  <!-- Publish static files -->
  <plone:static
      name="plonetheme.kaerumy"
      type="plone"
      directory="static"
      />

</configure>
